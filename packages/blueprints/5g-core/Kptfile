apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: 5g-core
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: Open5GS 5G SA core (NRF AMF SMF UPF + MongoDB + WebUI). Multus N2 AMF IP via setters in lab-defaults.
