apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: monitoring
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: Prometheus + Grafana (kube-prometheus-stack) and O-RAN ServiceMonitors.
